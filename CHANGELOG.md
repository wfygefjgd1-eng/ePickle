# PHUB Player 发布说明

> 说明：仓库内原先**没有**按版本的发布记录；Git 目前仅见大提交 `v1.6.0`。  
> 下文根据桌面成品包时间线、体积变化与源码注释整理，**1.5.x 细项为推断**，便于对照安装包。

---

---

---

## [2.8.29] - 详情页全量预热（与测速同步）+ 激进预加载默认开启（iOS + Android）

### 🚀 详情页全量预热（标准行为，无需开关）
- **全部站点 × 全部卡片**：首页预热从"前 2 个站点 × 前 2 条详情"扩展为**全部视频站点并行**，默认标签页列表内**每一张卡片**的详情页都在进首页后预热——点任何一张卡片都跳过详情网络请求，直接进播放器初始化
- **与最快域名测速同步进行**：预热在测速开始的同一时间窗内启动（首帧后约 0.6-0.8 秒），两路并行互不排队
- **随机页站点（Jable 等）预热正式生效**：预热为随机页信息流"计划"随机页并暂存（原激进模式专属机制，现为标准行为），信息流消费同一页并从下一页续抓——随机性保留，预热不白费
- **详情缓存扩容**：FeedDetailCache 上限 12 → 256，容纳全部站点的全量预热结果；App 退后台自动停止剩余预热，不空耗网络

### 🧪 激进预加载默认开启
- **开关默认打开**：新安装/未动过该开关的用户默认享受"详情页全量预热 + 排最前 2 个站点卡片首条视频的解码器预缓冲"；手动关闭过的不受影响（选择保留）
- 开关语义相应精简：详情层预热已是标准行为，该开关现在只控制**解码器预缓冲**这一层（点开近乎秒播的那层）

---

## [2.8.28] - 新增"激进预加载"（默认关）——点卡片近乎秒播（iOS + Android）

### 🧪 激进预加载（设置 → 激进预加载（实验），默认关闭）
- **多线程预热全部站点卡片**：开启后首页预热范围从"前 2 个站点"扩大到**全部视频站点并行**，每个卡片预热其首个视频的详情页，点开卡片直接跳过详情网络请求
- **解码器预缓冲（首创）**：对排最前的 2 个站点卡片，把"选流 → 播放器初始化 → 首帧缓冲"整条串行链也在首页空闲时提前完成（设备硬件解码器有限，故限 2 个，LRU 淘汰）；点开该卡片时播放页**直接收编预热好的播放器**，跳过整个"转圈圈"的初始化过程
- **随机页站点（Jable 等）同样生效**：预热时为随机页信息流"计划"一个随机页并暂存，信息流进入时消费同一页并从下一页续抓——既保住"每次进来内容都不同"的随机性，又让首条视频是热的
- **与原有预加载严格互不冲突**：播放器页按「条目 URL + 首候选流 URL」精确匹配收编，任何不匹配（画质上限变更、条目不同、未预热）都走原有冷启动路径；信息流内部的下一条预加载槽机制完全不动；App 退后台、关闭开关、预热被收编时立即释放全部预热解码器，不给信息流自己的解码器预算添乱

### ⚙️ 其他
- 新增 `FeedListSnapshot.sourcePage`：随机页信息流续抓从计划页的下一页开始，普通快照行为不变

---

## [2.8.27] - 测速更准更快 + 首轮域名可播性校验 + 预热前移（iOS + Android）

### ⚡ 最快域名机制（更准、更省）
- **首轮筛选不再被"假快"域名骗过**：HEAD 探测得到 4xx/5xx 时会补发一次 GET 确认 —— 真站（如 Jable，Cloudflare 对 HEAD 回 403 但页面正常）不受影响；而停放过期域名对 GET 也回 4xx，排名时将降到所有未测域名之后（但仍算"存活"，不算失败）。它不再凭毫秒级 403 抢到第一名，直接修复"JABLE 第一轮筛选出的域名点进去不能播放、手动换域名才能播"的问题
- **真实抓取成功会自动解除降级**：某域名一旦有真实请求成功，降级立即清零，排名始终跟随实际表现
- **探测队列大幅精简（测速不再啰嗦）**：每轮每站最多测 3 个域名 —— 排名第一的必测（保持延迟新鲜），健康备用域名不再每 30 分钟全部重测一遍（沿用已记录的评分，真实流量会持续校正），未知/失败域名优先补测。冷启动探测量约减半，日常稳态从"每站全量"降到"每站 1 个"，左上角"正在测速"徽标更快消失，也不再和真实内容抢带宽

### 🚀 首页预热前移（点卡片等待更短）
- **预热与测速并行**：首页列表预热从开屏 1.5s 提前到 0.6s —— 在域名检测进行期间就开始（之前是排队等测速跑完）。预热保持轻量：前 2 个站点卡片 → 各自列表 → 各自前 2 条详情，点开卡片直接跳过详情网络请求，进播放器初始化

---

## [2.8.26] - 标签页布局回归 + 域名检测纠错 + 镜像修复（iOS + Android）

### 🧭 标签页（站点"标签"页）
- **恢复左侧竖排布局**：v2.8.20 曾把窄屏标签栏改到顶部横滑，现恢复为标签靠左竖排、可上下滑动，点选后右侧显示对应内容（与宽屏一致）

### 🌐 域名检测（修复 JABLE 误报"域名异常"）
- **探测不再把 4xx 当作域名失效**：Jable 等 Cloudflare 站点对 HEAD 探测返回 403（反爬），但站点本身完全正常（App 内走原生渲染不受影响）。现在只要域名有 HTTP 响应（含 403/405/5xx）即视为存活并记录延迟；只有连不上/超时/DNS 失败才算失败
- **探测前先同步系统代理**：代理工具重启/换端口后，探测不再沿用失效的代理缓存
- **探测失败自动重新检测代理**：连接失败时清除 3 秒节流并立即重测，避免整轮探测都走死路
- **探测连接超时 3.5s → 5s**：代理链路 TLS 握手偏慢，过紧的超时会把"能用但慢"的域名误判为失败
- **修复 SOCKS5 代理导致全部请求失败**：Dart HttpClient 不支持 SOCKS，之前会把 `SOCKS5 host:port` 塞给底层导致每次请求抛 `HttpException("Invalid proxy configuration")` —— 表现恰好就是"代理像没连上"。现在检测到 SOCKS 时安全降级为直连（VPN/TUN 模式下流量仍走代理工具）

### 🐛 镜像与抓取修复
- **Pornhub**：非主域 URL（cn/rt/de…）不再把同一次失败重复记到 3 个候选域名头上（污染排名），也不再同一 URL 空转重试 3 遍；成败只记在实际访问的域名上
- **XVideos**：手动钉域名后，失败梯队按改写后 URL 去重（此前同一地址被逐个候选重试、一次失败就把钉住域名的失败计数打满）；列表锚定到实际服务的域名
- **Mitao**：列表/详情改走失败梯队，相对地址锚定到实际服务的镜像（此前失败转移后条目仍指向已挂域名）；详情页不再因首选域名死亡而无法播放
- **黄果**：专题卡片解析为空时直接复用已抓到的 HTML 走通用解析，不再重复请求同一 URL
- **播放页探测重试**：播放页在测速进行中打开时，测速结束后的"失败自动重试一次"此前因标志位未初始化而从不触发，已修复

---

## [2.8.25] - 域名检测提速 + 手动换域名 + 滑动快进回归（iOS + Android）

### ⚡ 域名检测（最快域名筛选）
- **启动后即开始测速**：探测从首帧后 4 秒提前到 0.8 秒，"发呆几秒"明显缩短
- **探测改跨站点轮询**：每一波对每个站点最多打 1 个探测请求，不再对同一域名家族瞬间连发 5-6 个请求（此前会触发风控 —— 这是"测速完成后反而报 无法访问源站"的根因）
- **测速结束后自动重试**：测速期间加载失败出错的卡片，测速完成（左上角转圈消失）后自动重试一次，无需手动点重试

### 🔧 域名异常提示与手动更换（新功能，默认关）
- **卡片置灰**：某站点所有域名都不可达时，首页卡片变暗置灰并显示"域名异常"
- **长按卡片更换域名**：设置 → 站点管理 → "长按卡片更换域名"开关（默认关闭）开启后，长按主页卡片可输入新域名；立即生效、跨重启保留（存于镜像排名同目录，隐私清理时会一并清除）
- Pornhub / XVideos / 通用站点全部支持：手动域名优先参与抓取与播放

### 🎬 播放器
- **竖屏左右滑动快进/快退回归**：v2.8.12 起该手势被限制为仅全屏生效，现恢复竖屏可用（竖屏翻页为纵向滑动，无冲突）
- **滑动幅度调小**：整屏滑动从 6 分钟降为 4 分钟（1/6 屏 ≈ 40 秒），短滑不再跳太远

### 🐛 站点修复
- **xHamster「热」标签修复**：/hottest 路径已下线（404），改为首页列表（实测 200、含视频卡片、支持翻页）；其余标签（/categories/{name}/、/newest）实测全部正常
- 全站标签核查：XNXX（hits/search-new/best/asian/tags）、TNAFlix（new/toprated/search）、Pornhub、XVideos 路径实测 200 正常；Jable 路径结构未变（对脚本请求 403 属 Cloudflare 反爬，App 内走原生渲染不受影响）
- **移除 Mitao（mitaohk.com）卡片**：不再出现在主页；其解析器保留供自定义站点复用

---
## [2.8.24] - 全面 bug 修复（iOS + Android）

### 🐛 播放器
- **悬浮按钮拖动修复**：设置/全屏/快进/静音按钮的拖拽范围按右/底锚定正确计算（此前右/底锚定的按钮几乎拖不动、且能拖出屏幕外）；旧版本保存的屏幕外位置会自动收回屏内
- **长按拖动残留修复**：长按按钮未移动就松手不再把上次拖拽残留（或原点）存成按钮新位置
- **手势中断修复**：长按被系统手势打断时按钮不再卡在"按住"状态、点按不再失灵
- **3 倍速竞态修复**：长按倍速被手势中断时自动还原；视频被冻结/回看时倍速强制还原 1x（此前回看可能"松鼠音"3 倍速播放）
- **快速滑动黑屏修复**：快速滑动时旧播放任务不再误杀已被冻结复用的控制器（此前回看上一个视频可能黑屏卡死）

### 🐛 信息流与列表
- **"加载更多"卡死修复**：整页全是已看过的视频时页码照常推进（此前永远重复请求同一页）
- **误报"无法访问源站"修复**（PHUB/X/Mitao）：全部去重完毕不再被当成网络故障弹错误提示
- **PHUB 信息流递进修复**：热闹/新标签每次加载覆盖不同页面组合（此前永远只抓固定 5 页，刷完 ~150 条后永远无新内容）
- **通用站点回退路径修复**：首个路径成功后，后续候选路径与 Cloudflare 原生渲染结果不再被错误复用的解析缓存挡住（此前搜索页被拦截时渲染了也白渲染）
- **黄果频道切换修复**：点缓存频道不再被上一个频道的错误页遮挡、不再被在途加载覆盖成别的频道内容
- **站点页返回修复**：全屏中按返回只退出全屏，不再杀掉所有播放并退出整个站点页
- **双击防抖**：首页卡片/搜索/目录/黄果页快速双击不再把同一个页面压栈两次

### 🐛 iOS
- **直播手动暂停保持**：手动暂停直播后回前台不再被强行续播（与 Android 行为一致）
- **空 200 响应兼容**：getBytes 不再把合法的空 200 响应当作错误（与 Android 对齐）

### 🐛 其他
- **隐藏站点保护**：隐藏站点页不允许隐藏最后一个视频站点（与首页开关策略一致），并给出提示
- **跳过片头设置**：写盘失败时回滚内存值（与其它设置一致，避免 UI 与下次启动不一致）
- **PHUB viewkey 兼容**：解析兼容 `ph` 前缀的现代 viewkey（此前只认纯 hex）

---
## [2.8.23] - 秒开优化：点卡片到出画面全链路提速（iOS + Android）

### ⚡ 点卡片 → 播放 提速
- **首页预取详情**：卡片列表预热后继续预取前 2 条视频详情（新增 FeedDetailCache 跨屏缓存，按 URL 键控，上限 12 条）——点卡片时详情已在本地，直接初始化播放器，省 1-3 秒网络等待
- **详情抓取去重共享**：initState 预取与播放路径共用同一 in-flight Future（原来同一详情会并发抓两次）——冷启动请求数减半
- **搜索播放器 initState 即发起首条详情抓取**：比播放路径提前一帧，同样通过共享 Future 复用
- **信息流列表落地即预取前 3 条详情**：首播与后续滑动全部命中温缓存

### ⚡ 域名检测提速（修复"检测最快域名 20 多秒"）
- **探测并发 4 → 8**，连接超时 6s → 3.5s：连不上的镜像不可能成为"最快域名"，不值得等 6 秒；35 个镜像的目录从 ~9 慢批降到 ~5 快批，典型耗时 ~5-10 秒
- **探测推迟 4 秒启动**：不再与首帧渲染/用户点开的第一张卡片抢带宽；等待期间请求走上次会话的持久化排名，探测只做精修
- 说明：v2.8.22 未改过探测逻辑；变慢的根因是 v2.8.0 起探测范围扩大到全部 35 个镜像，且排名过期（30 分钟 TTL）或上次有失败时每次启动都全量重探

### 🚦 带宽优先级
- **预载波改"详情并行、媒体串行"**：后台预缓存不再两个槽同时下载挤压当前画面（此前自家的预载可能触发卡顿降档）；上滑瞬时切换的体验不变
- 预载槽位刷新统一走 _restartPreloading()（详情并行预热 + 串行媒体填充 + 去重）

---
## [2.8.22] - 代码审查修复：WebView 播放复活 + 播放器稳定化（iOS + Android）

### 🐛 严重 Bug 修复
- **修复 WebView/直播播放完全失效（v2.8.21 引入）**：`_playInAppBrowser` 先 `_loadSeq++` 再拿调用方旧 seq 做守卫，`seq != _loadSeq` 恒真导致该方法总是提前返回——Stripchat/直播与所有"无流走网页播放"的视频都卡在黑屏/无限转圈。现在方法内部自取新 seq
- **修复未提交的半成品重构导致无法编译**：preload 三槽字段被删但 70+ 处引用未迁移；现以统一的 `PreloadSlot` 槽位列表完成重构（两个播放器共用，删除约 450 行重复代码）
- **修复质量切换/自动降档从 0:00 重播**：切清晰度与卡顿自动降档现在会记住原播放位置并 seek 回去（手动切换与自动降档均生效）
- **修复进度条拖动被系统手势打断后永久冻结**：补 `onHorizontalDragCancel`，`_seeking` 不再卡死
- **修复滑动进度提示乱码**：`+5绉?鈫?0:05` → `+5秒 → 0:05`（GBK 双重损坏的字节）
- **修复黄果频道页切换频道时偶发崩溃**：`ScrollController.offset` 在无滚动视图挂载时抛 StateError，改为 hasClients 守卫

### 🎬 播放器稳定化
- **预载双启动泄漏修复**：同一槽位并发填充会泄漏一个已初始化解码器；槽位现在带 inFlight 锁
- **冷启动详情预取真正生效**：`_prefetchDetail` 的 `_canRun` 守卫与注释矛盾，首帧预取从未执行
- **路由返回/前台恢复恢复音量**：didPushNext 静音后返回会一直无声，startPlaying 现在重设音量
- **速度标签不串台**：新视频不再短暂显示上一个视频的 Kbps
- **总时长标签可实时更新**：`FeedProgressBar.totalTime` 改为 ValueNotifier，HLS 时长解析后不再停留 0:00
- **搜索列表进度条不再整页重建**：速度/总时长改 ValueNotifier，仅标签局部刷新（5 次/秒 → 小部件级）
- **200 条上限 rebase 后预载重新武装**：`_preloadWaveSeq/_preloadWaveIndex` 重置

### 📱 平台与构建
- **Android 黄果封面黑块修复**：实现缺失的 `getBytes`（含 AES-128-CBC 解密，与 iOS 对齐）——黄果源封面在 Android 上一律黑屏
- **Android 隐私擦除移出主线程**：递归删除 + 同步 commit 不再卡 UI（ANR 风险）
- **Android 排队中的 HTTP 请求在 Activity 销毁时兜底回复**：Dart 侧不再永久挂起
- **Android Stripchat 视图释放竞态修复**：旧实例 dispose 不再误删新实例引用
- **删除无用媒体存储权限**（WRITE/READ_EXTERNAL_STORAGE、READ_MEDIA_VIDEO）
- **iOS 非 UTF-8 页面正文不再返回空串**：与 Android 行为对齐
- **iOS 删除无效的 dispose() 钩子**（FlutterPlatformView 无此协议方法）
- **CI 固定 Flutter 3.47.1**；ios-ipa.yml 的 xcodebuild 回退不再被 pipefail 吞掉；删除与 release-build.yml 抢发布命名空间的 iOS publish job

---
## [2.8.20] - 播放速度优化 + 稳定性修复（iOS + Android）

### ⚡ 首次播放提速
- **详情抓取超时 12s → 8s**：详情请求更早超时失败、触发降级
- **Player 初始化超时 12s → 8s**：HLS 初始化更早重试
- **多候选流循环：最多只试 2 个**（原来可能试 3 个，每个 12s 串行）
- **详情并行预取**：进入 SiteFeedPage 时在 initState 中 fire-and-forget 预取第一个视频详情，首帧播放从 ~7s 降至 2-4s
- **首个 tab autoStart 改为 true**：提前触发预热逻辑

### 🐛 Bug 修复
- **修复「候选流循环」与「isLikelyPreview continue」冲突 bug**：当第一个流返回试看片段时，`attempt > 1 break` 导致第二个候选（正片）被跳过，用户被错误提示"开 TUN"；改为 `for (var i = 0; i < candidates.length && i < 2 && player == null; i++)`
- **修复 SiteFeedPage 与 autoStart 双触发冗余**：删除重复的 addPostFrameCallback(startPlaying)

### 🧹 冗余与性能
- **删除 _multiPreload 死代码**：`preloadSlotCount` 是 const 2，该 getter 恒返 true
- **ValueNotifier 写入移出 setState**：避免多余状态重建
- **cache_manager 单飞锁注释修正**：改为更准确的"Future 缓存单飞模式"

---

## [2.8.12] - 代码审查修复：bug 修复 + 稳定性 + 冗余清理（iOS + Android）

### 🐛 Bug 修复
- **修复「跳过片头」档位规则相互覆盖**：编辑任一档的阈值/秒数不再连带覆盖更高档（switch 缺 break 导致穿透）
- **修复自动横屏完全失效**：重力感应切换动作未生效（confirmAction 缺 break，进入横屏后立即被清空）
- **修复滑动列表满载后播放错误视频的竞态**：列表窗口裁剪后没有使在途播放失效
- **修复首页预加载偶发失效**：一次性定时器在应用未完全唤起时触发会永久跳过预热

### ⚡ 稳定性与提速
- **详情抓取增加 12 秒超时**：源详情请求不再让滑动播放在网络挂起时卡死
- **首页/镜像测速启动清理提前注册**：确保每次冷启动的磁盘清理与域名测速按预期时机执行
- **观看历史/镜像/缓存多处并发与内存加固**（见各文件 diff）

### 🧹 冗余与性能
- **视频 viewkey 正则改为编译一次**（feed 热路径每项多次调用的重复编译消除）
- **短剧集数缓存加容量上限**（长会话不再无限占内存）
- **镜像排名索引归一化**（避免写入 -1 的失效索引）

---

## [2.8.1] - 测速悬浮提示 + 排名数据持久保护（iOS + Android）

### 🔍 测速状态提示
- 后台对域名测速时，**首页左上角显示悬浮小标**（转圈 + "正在测速最快域名…"）
- 测速完毕**自动消失**——看到左上角没有东西，就知道检查完成、软件可正常使用
- 无需测速时（排名仍新鲜）不打扰，30 分钟后过期或测出失败才会再次出现短暂提示

### 🛡️ 排名数据保护
- 确认"定时清除痕迹/缓存"（每次启动自动清理）只清除 WebView 缓存与临时文件，**不触碰测速排名**
- 手动"清除痕迹并退出"（核弹级）：排名数据（仅延迟统计，无浏览痕迹）在 iOS/Android 双端**豁免保留**，清完不用重新学

### ♻️ 体积与内存
- 排名数据为固定大小的小 JSON（每站点每域名 4 个数字），覆盖更新、绝不增长
- 探测连接复用同一客户端，无连接/回调泄漏；App 不会越用越大

---

## [2.8.0] - 每个网站自动优选最快域名（iOS + Android）

### ⚡ 域名优选（本次核心）
- **每个网站、每张卡片都会自动选出最快的域名**：App 内置大量镜像域名（Pornhub 6 个、XVideos 3 个等），现在为每个站点维护一份**持久化的域名排名**
- 后台**轻量测速探针**（HEAD 请求，走与真实请求相同的代理通道）测量每个域名的首字节延迟，30 分钟 TTL，失败立即重测
- **访问策略**：每个网站的 feed/搜索/详情一律**优先访问排名第一的最快域名**；若它失败，**其余所有域名并行上场**竞速，谁先成功谁接管——任何域名都不会被丢弃
- **越用越准**：每次真实请求的成败与耗时都会回写排名（EWMA 平滑延迟 + 连续失败降级），排名跨会话保存（iOS/Android 分开记忆）
- 彻底清除/换网络后自动重新测速；自定义站点同样参与排名

---

## [2.7.1] - 缓存度量修正与收尾优化（iOS + Android）

### 缓存治理
- **缓存大小统计改为统计整个临时目录**：之前一旦出现图片缓存目录就只统计它，临时目录/其他缓存增长被“藏起来”，500MB 上限形同虚设；现在能真实触发阈值清理
- **“彻底清除”后重建 URLCache**：隐私擦除不再把 URLCache 永久清零（避免未退出场景下每个视频重新下载）；每次进入自动清理保持不变

### 稳定性与健壮性
- 观看历史：单条损坏记录不再导致**整份历史被丢弃**（逐条容错解析）；文件路径只在启动时解析一次、备份排除标记只调用一次
- 翻译器内存缓存与磁盘一致上限 400 条（长滚动会话不再无限占 RAM）
- 设置加载失败时的冗余默认值兜底块删除（字段初始值本就一致，纯死代码）

---

## [2.7.0] - 启动提速、每次进入自动清缓存、稳定性修复（iOS + Android）

### 🧹 每次进入自动清理缓存
- **启动即清理**：App 每次冷启动都会自动清除缓存（WebView 磁盘/内存缓存、URLCache、图片缓存、临时目录），磁盘占用不再无限增长；设置、观看历史、登录 Cookie、钥匙串全部保留，App 依然开箱即用
- 清理改为“首帧后异步执行”，不会拖慢启动画面

### ⚙️ 设置项调整
- **移除「自动跳过无信号频道」开关**：该行为改为始终开启（频道无信号时自动切下一个），无需再在设置里配置

### ⚡ 启动与滑动提速
- App 启动时三个配置加载改为**并行**，冷启动等待时间明显缩短
- 首页 3 个频道预加载改为**并发**抓取（原来逐个串行）
- 底部导航栏毛玻璃模糊半径 16→8，滚动跟手度提升（低端机更明显）
- 列表页解析后的 HTML 不再重复解析两次；Pornhub 列表 DOM 解析保留为兜底，常规解析更省 CPU

### 🐛 稳定性修复
- 修复快速滑动时“冻结核”被重复销毁导致崩溃的竞态（feed + 搜索）
- 修复播放失败错误从定时器/翻页回调逃逸为未处理异常、可能整页崩溃的问题
- 修复开始播放失败会反复逐级降清晰度的“级联降档”问题（现在每条仅自动降一次）
- 修复搜索页“黄果短剧”一集播完**不会自动接下一集**的失效逻辑
- 修复拖进度条/快进时 seek 挂起会把进度条卡死的隐患（加超时兜底）
- 修复 iOS 原生 WKWebView 渲染成功却偶发被误报为超时的问题
- 修复 macOS 上 URLSession 渲染回调可能永不释放渲染槽的问题（Dart 侧加超时）
- 修复代理软件重启后列表请求连续超时、无法自动恢复的问题（超时后立即重测系统代理）
- 修复多个站点镜像并发探测：胜出后**立即取消**失败探测，释放 socket 与渲染槽

### 🧹 冗余清理
- 删除未使用的死代码 5 个文件：HomeShell、DraggableButton + ButtonPositions、WatchHistoryPage、SiteSearchPage
- 删除不可达的“第 4 个预加载槽”注释误导；清理空分支、重复方法、失效注释

### 📄 说明
- 详细发布说明见 `RELEASE_NOTES_v2.7.0.md`

---

## [2.6.6] - 代理网络优化（iOS + Android）

### 代理感知
- **iOS 系统代理检测**：Dio 现可通过 `CFNetworkCopySystemProxySettings` 读取 iOS 系统代理（Shadowrocket / Quantumult X 系统代理模式），解决「仅开系统代理时国外站点列表加载超时转圈」的问题；TUN 模式自动保持直连
- **代理缓存自愈**：请求超时/连接失败会自动标记代理缓存失效，下次请求前重新检测系统代理，代理软件重启、切换端口后无需进设置手动「重新检测代理」

### 加载提速
- **单请求超时兜底**：PH / XVideos / Mitao / 通用站点的 HTML 请求统一 10 秒单请求上限，超时真正取消底层 socket（此前最坏可挂 28 秒且无法取消）
- **转圈时长收敛**：代理不通时首屏加载从最长 ~30 秒降到 ~10 秒内出结果（成功或错误提示），不再让失败连接吃满全部超时

### iOS 原生
- WebView 渲染请求（`renderGet`）改用前台窗口挂载，修复 Scene 生命周期下窗口为 nil 导致反 Cloudflare 兜底容易超时
- 直播间视频已显示/加载失败后停止每秒注入 JS（省电省 CPU，失败后不被页面意外视频覆盖提示）
- Info.plist 增加 `NSAllowsArbitraryLoadsInWebContent`，允许 http:// 站点在 WKWebView / URLSession 中正常加载

---

## [2.5.1] - Android 功能对齐与稳定性修复

### Android 对齐
- Android `renderGet` 现使用隐藏 WebView 执行 JavaScript、等待浏览器挑战并采集最终 DOM，与 iOS WKWebView 行为一致
- Android 浏览器渲染同步返回最终 URL、页面资源与站点 Cookie，修复动态站点仅 iOS 可解析的问题
- 原生 HTTP 与 WebView 共享 Cookie 会话，重定向和后续详情请求行为与 iOS 对齐

### 稳定性
- Activity 销毁时取消未完成的渲染和网络请求，防止 WebView/回调泄漏
- 原生并发请求使用线程安全任务表，避免多页面并行加载时的数据竞争

---

## [5.2.1] — 桌面端关键 Bug 修复与稳定性优化

### 关键 Bug 修复
- **RealtimeTranslator 回调异常**：修复构造函数传入 IPC 路径而非回调函数，导致字幕功能完全不可用
- **事件循环无法退出**：修复窗口关闭时 asyncio 事件循环未停止，导致僵尸线程残留
- **下载进度回调淹没事件队列**：修复每次回调产生 2 个 `root.after()` 调用，导致 GUI 卡顿
- **搜索数量输入崩溃**：修复搜索数量框为空或非数字时程序崩溃
- **全局异常处理器 NameError**：修复 `__main__` 块中 `_tb` 未导入，导致异常处理器自身崩溃
- **段缓存内存泄漏**：修复 HLS 代理缓存无限增长，现限制 LRU 上限 50 段
- **ffmpeg 子进程状态未重置**：修复字幕翻译 ffmpeg 启动失败时 `running` 标志未置 False

### 优化改进
- **下载进度节流**：进度回调从每次改为 200ms 间隔，减少 GUI 事件队列压力
- **Toast 提示时长**：从 500ms 提升至 1500ms，用户可完整阅读提示
- **批量翻译可靠性**：改用逐条翻译 + 0.15s 间隔，避免 Google 翻译 API 返回换行导致结果错位
- **IPC 路径唯一性**：每个实例使用 UUID 生成唯一 pipe 名，避免多实例冲突
- **焦点抢占频率**：从 500ms 降至 5000ms，减少对用户操作其他窗口的干扰
- **错误日志统一**：统一输出至 `~/phub_logs/` 目录，自动创建不依赖硬编码路径
- **全局异常钩子**：新增主线程/子线程/Tk 回调三级异常捕获，日志写入 error.log
- **裸 except 语句**：全部替换为 `except Exception:`，允许 `KeyboardInterrupt` 正常传播
- **搜索输入校验**：增加 1-100 范围检查，无效值自动回退默认值 10

### 线程安全修复
- **下载协程 Tkinter 线程安全**：修复 `do_download()` 中 `self._log()` 在协程（事件循环线程）内直接访问 Tkinter 控件的问题，改用 `self.root.after(0, ...)` 调度到主线程执行
- 三个 GUI 文件（phub_gui.py、phub_gui_v1.py、phub_gui_v2.py）均已修复

### 代码清理
- 移除未使用的 `_last_progress_update` 变量

### 窗口关闭安全
- 所有异步回调（搜索、详情、推荐、下载）增加 `self._running` 检查，防止窗口关闭后仍尝试更新 GUI 导致 `TclError`
- 修复 `is False` 比较为更规范的 `not` 运算符

---

## [1.7.3] — 代理状态一眼懂 + 冷启动更快 + 播放失败说明

### C 代理状态
- 设置顶部展示「当前：直连 / 系统未下发代理 / host:port」
- 补充列表通/播不通时的说明（SOCKS vs HTTP、TUN）

### B 冷启动
- 首屏只试 2 个列表 URL、约 16s 总超时，成功即停
- 失败更快进入空态，少长时间转圈

### A 播放失败
- 初始化失败时按代理状态提示：SOCKS 常不跟播放器 / 可开 TUN 或改 HTTP

---

## [1.7.2] — Tab 保活 + 失败提示 + 播放尽量走 HTTP 代理

### 体验
1. **Tab 切换**：`IndexedStack` 保留列表状态，切走暂停并释放播放器（省内存），切回不白屏重拉
2. **加载失败**：错误文案说明可「重新检测代理 / TUN / 手动代理」；空态增加「打开网络代理设置」
3. **播放与代理**：Android 在配置 HTTP 代理时同步 JVM `http(s).proxy*`，尽力让 ExoPlayer 也走代理（SOCKS/部分机型仍可能仅列表通，可开 TUN）

---

## [1.7.1] — 关闭卡顿检测与自动/提示降画质

### 播放
- **移除**卡顿检测弹窗与一切自动/半自动降清晰度
- **移除**开播失败时的清晰度回退链（不再自动试低清）
- 画质**仅**用户在设置中手动选择；卡了就自己去设置换
- 后台切换不再触发任何画质相关逻辑

---

## [1.7.0] — 跟随系统代理（不写死主机/端口）

### 为什么
- 很多软件默认能访问，是因为用了 **系统代理** 或 WebView。
- Dio 默认直连时：非 TUN 下浏览器能开、本 App 不能。

### 变更
- 启动时 **只读取系统/环境代理**（Android ConnectivityManager、系统属性、ProxySelector、环境变量）
- **不写死** `10.0.2.2` / `7890` 或任何厂商端口
- 读到 → Dio 走该代理；**读不到 → 直连**（干净手机/模拟器也正确）
- 设置里可「重新检测」或手动填写；可关闭「使用系统/本地代理」
- **无内置节点**

### 说明
- 仅「浏览器代理」、系统未下发代理时，检测仍可能为空 → 需 TUN/系统代理，或手动填
- 换手机/模拟器：自动以当前设备系统配置为准

---


## [1.6.9] — 可选本地代理（非 TUN 可用）

### 网络
- 设置 → 本地代理（旧版默认关）
- HTTP / SOCKS5；模拟器 `10.0.2.2`；真机 `127.0.0.1`

---


## [1.6.8] — 画质进设置 + 全屏布局 + 卡顿提示防误报

### UI
- **手动画质**进入设置（右上角 / 全屏左上角 ⚙），右侧竖条**只保留静音**
- 全屏：**左上角设置（含画质）**，**右上角退出全屏**
- 设置内：跳过片头、卡顿时询问降画质、画质列表

### 卡顿提示
- **后台再进前台不再立刻弹**（暂停/隐藏时清计数 + 回前台 5s 冷却）
- 开播约 4s、seek 后约 3s 内不检测，减少误报
- 确认降级**仅本条**（session cap），不改全局默认画质

---

## [1.6.7] — 卡顿时提示降清晰度（用户确认才切换）

### 功能
- 播放中若检测到卡顿（进度几乎不走）：弹出「是否切换到更低清晰度」
- **点切换** → 按更低清晰度重开当前视频
- **点继续 / 关闭** → 保持当前画质继续播，且本条不再弹
- **不会**自动降级；**不会**网好自动升回
- 推荐流与搜索竖屏播放均支持

---

## [1.6.6] — 关键失败提示（不限流翻译）

### 变更
- **不限制标题翻译速度/数量**（保持现有批量翻译）
- 播放详情失败、无地址、地区不可用、初始化失败：toast 中文说明
- 清晰度降级成功：提示「已切换到 xxxp」
- 列表加载失败：空列表用友好错误文案；已有列表时 toast「加载更多失败」
- 搜索页错误文案经 `friendlyError` 转成中文提示（403/超时/网络等）

---

## [1.6.5] — 修复 CI 编译失败

### 修复
- Android：`VideoPlayerController?` 空安全编译错误（清晰度降级循环赋值后 analyzer 不提升）
- iOS CI：`CODE_SIGNING_ALLOWED=NO`（`--no-codesign` 仍要求 Development Team 的问题）

---

## [1.6.4] — 稳妥优化（不删功能）

### 保留全部现有功能，仅增强稳定性 / 性能
- **flashvars 多策略解析**（主正则失败再试备用模式）
- **清晰度降级重试**：首选画质初始化失败时自动试更低/其他流
- **列表封面解码限尺寸**（`memCacheWidth/Height`），长列表更省内存
- **标题翻译优先当前附近条目**，仍批量译完全部新加载项

---

## [1.6.3] — 修复标题中文翻译

### 修复
- **播放页 / 推荐流标题自动译中文失效**：统一 Dio 默认 `plain` 后，Google 翻译返回被当纯文本，JSON 解析失败 → 一直显示英文。
- 翻译请求改为 `ResponseType.json`，并兼容 string 体再 `jsonDecode`。
- 推荐流列表加载后 **批量译标题**；播放时译当前标题并写回列表项。
- 搜索竖屏播放页同样调用标题翻译。

---

## [1.6.2] — 纠正产品身份（源码）

### 重要
- **应用名固定为 PHUB Player**（iOS `CFBundleDisplayName`、文档、CI 发布名）
- **移除误并入的「隐私浏览器」入口与相关 UI**（那是另一个项目）
- 仅保留视频播放器主路径

### 仍保留的 1.6.1 优化
- 统一 Dio / UA
- Feed 减负（约 5 页、并发 3）
- Android 15：关 Impeller、不全屏强横屏

---

## [1.6.1] — 优化（曾误把隐私浏览器并进；已由 1.6.2 纠正）

### 通用
- 列表更快、统一网络层等（见上）
- ~~双入口隐私浏览器~~ → **1.6.2 已移除**

---

## 版本与产物一览（桌面 `C:\Users\96335\Desktop`）

| 版本 | Android APK | iOS IPA | 说明 |
|------|-------------|---------|------|
| 1.5.3 | 有（约 53.3 MB） | 无 | 仅安卓 |
| 1.5.4 | 有（体积与 1.5.3 相同） | 无 | 仅安卓 |
| 1.5.5 | 有（体积微变） | 有（约 7.5 MB） | 双端 |
| 1.5.6 | 有（体积再微变） | 有 | 双端 |
| 1.5.7 | 有（当前 1.5 线最新包） | 有 | 双端 |
| 1.6.0 | 源码已标 `1.6.0+15`，桌面未见同名成品包 | 同左 | 产品形态切换（见下） |

**结论（与「只改安卓」相关）：**

- **1.5.3～1.5.4**：只有 Android 包 → 明确是安卓侧迭代/试修。
- **1.5.5～1.5.7**：每次 Android 打完后约数分钟内会打 iOS IPA → 更像**同源码一起出包**，iOS **未必有独立功能变更**，多为**同步打包抬版本号**。
- 若当时目标是修 **Android 15 闪退**，iOS 连打多版**确实没有单独修 iOS 的必要**；保留 IPA 主要是侧载分发/版本号对齐。

---

## [1.6.0] — 历史提交（git `f5a12d3`，已纠正）

曾短暂把「隐私浏览器」入口合入本仓库（**误**）。**1.6.2 起本仓库恢复为纯 PHUB Player。**

---

## [1.5.7] — 2026-07-23（成品包）

### Android
- 继续 1.5.x 修复线收尾打包（体积相对 1.5.6 略有变化）。
- **推断目标**：巩固 Android 15 相关稳定性（见下方「跨 1.5.x 的安卓修复点」）。

### iOS
- 同步打出 IPA（时间紧挨 Android 包）。
- **推断**：无独立 iOS 缺陷说明；主要为版本号对齐 + 侧载分发。

---

## [1.5.6] — 2026-07-23（成品包）

### Android
- 再次微调打包（体积略降）。
- **推断**：仍属 Android 15 / 播放器稳定性修补或配置微调。

### iOS
- 同步 IPA；**推断**同 1.5.7，无单独 iOS 功能 changelog。

---

## [1.5.5] — 2026-07-23（成品包）

### Android
- 体积相对 1.5.4 有极小差异（非空包拷贝）。
- **推断**：继续修安卓闪退/渲染相关问题后的有效构建。

### iOS
- **本线首次出现 IPA**（约 Android 之后 13 分钟）。
- **推断**：开始「安卓修完顺手打 iOS」，而非 iOS 专修。

---

## [1.5.4] — 2026-07-23（成品包）

### Android only
- 与 1.5.3 **文件大小完全一致**（可能是同构建重打、仅改元数据，或改动未影响包体）。
- **推断**：针对 Android 15 闪退的再尝试打包。

### iOS
- 无包。

---

## [1.5.3] — 2026-07-23（成品包）

### Android only
- 可见的 1.5.x 起点安装包。
- **推断**：进入「密集修 Android 15」阶段的第一枪。

### iOS
- 无包。

---

## 跨 1.5.x 的安卓修复点（源码中可核对）

以下写在代码注释/清单里，**高度吻合「修 Android 15 闪退」**：

1. **关闭 Impeller，改用 Skia**  
   - `AndroidManifest.xml`：`io.flutter.embedding.android.EnableImpeller = false`  
   - 注释：Impeller 会导致 Android 15 横屏模拟器 / 宿主进程挂掉。

2. **避免在 Android 上强制横屏**  
   - `player_chrome.dart`：全屏时仅 iOS 设置 `preferredOrientations`；Android 保持设备当前方向。  
   - 注释：Android 上强改方向可能 hard-crash Android 15 模拟器 / GPU host。

3. **网络 / 权限**  
   - 清稿流量、媒体权限等常规移动端配置（非 15 专有，但是播放器基线）。

这些修改对 **iOS 无对等闪退叙事**，因此 **iOS 多版本更像版本号 + 重打包**，而不是「iOS 也修了四五版同等问题」。

---

## 产品基线（1.5 播放器阶段，简述）

- 纯客户端抓取（无自建后端、无内置代理；依赖系统 VPN）。
- 推荐 / 搜索 / 详情 / HLS 播放（`media_kit` 或 `video_player` 等依版本）。
- 多源相关代码后续进入仓库（如 `phub_api` / `xvideos_api` / `mitao_api`）。
- 不做会员登录；站点改版可能导致解析失效。

---

## 维护约定（建议）

以后每打一个对外安装包，在本文件顶部追加一节，至少写：

```text
## [x.y.z] — YYYY-MM-DD
### Android
- 改了什么 / 修了什么
### iOS
- 有实质修改 → 写清
- 仅同步打包 → 写「同步重打包，无独立变更」
```

避免再出现「安卓连修多版、iOS 只抬版本号却无说明」的情况。

---

## 自用签名说明（正式证书可跳过）

**只自己用 / 小范围侧载：用 debug 签名完全够。**

当前 `android/app/build.gradle.kts` 里 release 仍指向 debug key，因此：

```bash
flutter build apk --release
```

打出来的包和日常 `flutter run` 是同一套签名，**卸载重装可覆盖**（同一台机器、同一 debug keystore）。

### 什么时候才需要「正式签名」
- 要给很多人分发、且希望 **换电脑打包仍能覆盖安装**
- 或以后要上架商店（本项目不建议上架）

### 若以后真要正式签名（可选，费事）
1. 本机生成一次 keystore（只做一次）：
   ```bash
   keytool -genkey -v -keystore phub-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias phub
   ```
2. 在 `android/` 下放 `key.properties`（**勿提交 git**）：
   ```
   storePassword=***
   keyPassword=***
   keyAlias=phub
   storeFile=../phub-release.jks
   ```
3. 在 `build.gradle.kts` 的 `release` 里改用该 signingConfig。

**结论：个人小范围分发 → 保持 debug 签名，不必折腾正式证书。**

---

## 免责声明

本软件仅供学习研究；使用者自行合规与网络访问；勿上架应用商店商用。详见 `README.md`。
