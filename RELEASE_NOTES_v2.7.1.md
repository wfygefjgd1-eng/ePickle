# ePickle v2.7.1 — 收尾优化（缓存度量修正 + 健壮性）

> 在 v2.7.0 基础上的小版本：修正“500MB 缓存上限形同虚设”的度量范围问题，并加固观看历史/翻译器/设置加载的健壮性。

## 🧹 缓存治理修正

- **缓存大小统计改为整个临时目录**：此前一旦 `libCachedImageData` 图片缓存目录存在，统计就只看它，临时目录与其他缓存（`flutter_cache_manager`、临时下载）的增长被隐藏，500MB 阈值几乎不会触发；现在按整个临时目录统计，阈值清理真实生效
- **“彻底清除”不再永久清零 URLCache**：隐私擦除后重建为默认容量，未退出的场景不会导致每个视频重新下载
- 每次进入自动清理（v2.7.0 引入）保持不变：清 WebView/URLCache/图片/临时缓存，保留设置、历史、Cookie、钥匙串

## 🛡️ 健壮性加固

- 观看历史：单条 JSON 损坏不再丢弃整份历史（逐条容错）；文件路径启动时解析一次、备份排除标记只调用一次
- 翻译器：内存缓存与磁盘一致上限 400 条，长滚动会话不再无限增长 RAM
- 设置/布局加载：删除重复默认值的死代码兜底块（字段初始值本就一致）

## 全量更新日志

见 [CHANGELOG.md](./CHANGELOG.md)

## 免责声明

本软件仅供学习研究和技术交流目的使用。使用者须自行遵守所在地法律法规。严禁用于商业用途或非法目的。