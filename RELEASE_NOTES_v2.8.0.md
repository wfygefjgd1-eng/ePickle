# ePickle v2.8.0 — 每个网站自动优选最快域名

> 适用平台：iOS + Android（侧载）
> 发布方式：GitHub Actions 自动构建并发布（APK / IPA）

---

## ⚡ 核心功能：域名优选

App 内每个站点都内置了多个镜像域名（Pornhub 6 个、XVideos 3 个、蜜桃 2 个等）。v2.8.0 让 App **为每个站点自动选出最快的域名，并优先访问它**：

- **后台测速**：进入 App 后静默对每个站点的所有域名发轻量探测（HEAD 请求，走与真实请求相同的代理通道），测量首字节延迟；30 分钟有效，失败立即重测
- **最快优先**：访问任何站点的列表/搜索/详情时，第一个请求就直接打向排名第一的最快域名，不再有"先转圈再出内容"
- **全域名兜底**：最快域名失败时，其余所有域名并行上场竞速，谁先成功谁接管——任何域名都不会被跳过
- **越用越准**：每次真实请求的成败/耗时都回写排名（指数加权延迟 + 连续失败降级），iOS/Android 分开记忆，跨会话生效
- **自适应恢复**：更换代理/网络后自动重新测速；被墙或 403 的域名自动沉底

## 使用

无需任何设置，打开即生效。首次进入后约几十秒完成后台测速，之后每次访问都走最快域名。

## 免责声明

本软件仅供学习研究和技术交流目的使用。使用者须自行遵守所在地法律法规。严禁用于商业用途或非法目的。